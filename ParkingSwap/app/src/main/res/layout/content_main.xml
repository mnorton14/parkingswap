<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.jaskiratsingh.parkingswap.MainActivity"
    tools:showIn="@layout/activity_main">

    <Button
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:id="@+id/Find"
        android:text="Find Lots"
        android:textColor="@android:color/background_light"
        android:background="?attr/colorPrimary"
        android:onClick="onButtonClickListener"
        android:layout_alignBottom="@+id/Leave"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="52dp" />

    <Button
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:id="@+id/Leave"
        android:text="Leave"
        android:background="?attr/colorPrimary"
        android:textColor="@android:color/background_light"
        android:layout_alignBottom="@+id/Quick_Find"
        android:layout_alignLeft="@+id/Find"
        android:layout_alignStart="@+id/Find"
        android:layout_marginBottom="52dp" />

    <Button
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:id="@+id/Quick_Find"
        android:text="Quick Find"
        android:textColor="@android:color/background_light"
        android:background="?attr/colorPrimary"
        android:layout_centerVertical="true"
        android:layout_alignLeft="@+id/Leave"
        android:layout_alignStart="@+id/Leave" />

</RelativeLayout>
